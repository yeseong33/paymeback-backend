# Server
server.port=8080

# Timezone (DB: UTC 저장, JSON 응답: KST 변환)
spring.jackson.time-zone=Asia/Seoul
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# DB
spring.datasource.url=jdbc:postgresql://localhost:5432/pay-me-back
spring.datasource.username=admin
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# jpa
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.timeout=3000ms

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:ohyeesung@gmail.com}
spring.mail.password=${MAIL_PASSWORD:dzveiddqraqfimvz}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT
jwt.secret=${JWT_SECRET:your-secret-key-here-make-it-long-enough-for-security}
jwt.expiration=86400000

# QR Code
app.qr-code.expiration-minutes=5
app.payment.timeout-hours=24
app.otp.expiration-minutes=5
app.otp.length=6

# Logging
logging.level.com.dutchpay=DEBUG
logging.level.org.springframework.security=DEBUG 

# Security
spring.security.basic.enabled=false
management.security.enabled=false